[Unit]
Description=BBR Squash — backup timer (every 3 days)

[Timer]
# Every third day-of-month at 03:30 local server time.
OnCalendar=*-*-1/3 03:30:00
# Run on next boot if the server was off when the timer fired.
Persistent=true
Unit=bbr-backup.service

[Install]
WantedBy=timers.target
